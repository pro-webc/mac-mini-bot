# pytest / ruff のみ（パッケージ配布用メタデータは置かない）
# 開発: pytest / ruff check（パスは README「品質・Lint」または CI 参照）
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra -q"
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py39"
src = ["config", "modules", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# ロギング初期化後にアプリ import するため
"main.py" = ["E402"]
