**プラン: STANDARD-CP（コーポレート・複数ページ・6ページ想定）** — 手順7-3 は擬似 SPA（`useState`）の単一ファイル出力になりやすい。続く共通 `preface_shared` と `refactoring_instruction` に従い、**ルート相対パス付きフェンスでソースのみ**出力すること。リファクタ後はファイルベースルート（`app/**/page.tsx`）と `app/components/**` に分割し、本番運用しやすい構成にすること。
