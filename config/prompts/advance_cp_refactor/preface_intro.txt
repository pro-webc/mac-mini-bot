**プラン: ADVANCE-CP（コーポレート・複数ページ・12ページ想定）** — 手順7-3・7-4 は擬似 SPA（`useState`）の単一ファイル出力になりやすく、ページ数が多いためヘッダーでサービス等をドロップダウン表示する指示が含まれる。続く共通 `preface_shared` と `refactoring_instruction` に従い、**ルート相対パス付きフェンスでソースのみ**出力すること。リファクタ後はファイルベースルート（`app/**/page.tsx`）と `app/components/**` に分割し、本番運用しやすい構成にすること。
